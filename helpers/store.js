const fs = require('fs');
const util = require('util');

function getNote () {} //Function to get notes from json file 

function storeNote () {} //funtion to add new notes to json file 


module.exports = {getNote, storeNote}